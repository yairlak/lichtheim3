# SMOKE ARTIFACTS — NOT RESULTS

Produced by `run_gate_route_audit.py --smoke` to validate the pipeline end to end
(400 items, one state, `W3_SRC`). They exist to show the machinery runs and that
the contract's power rule and `STRUCTURALLY_EMPTY` path fire correctly.

**They are not evidence and must not be cited, plotted, or summarised.**
They are quarantined here so they cannot be mistaken for the full pass, which writes
to `../figure_source_data/` and `../summary_metrics.json`.
